language: python
python:
  - "2.7"
  - "3.6"

sudo: required

before_script:
  - pip install sphinx pygments

script:
  - make -C docs/ install-lexer build-doc-warning

notifications:
  email:
    recipients:
      - atzeinicola@gmail.com
    on_success: change # default: change
    on_failure: always # default: always
